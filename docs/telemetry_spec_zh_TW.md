# オシハピ｜推し買い診断 Telemetry（匿名資料）規格（MVP）

目的：
- 用於優化規則/權重/閾值、題目品質、UX 的匿名資料蒐集
- 不收集個資（不登入、不要 email、不主動保存 IP）
- 預設不送，僅在「使用者明確按下送信」才送出

## 送出時機（MVP）
- 結果頁新增「学習のために匿名データを送信」卡片
- 只有使用者按下按鈕時才 `POST /api/telemetry`
- L1 回饋（買った/保留した/買わなかった/まだ）按下時：若同意已 ON，則也送一筆（可選）
