# Overlay usage (Windows / PowerShell)

This zip is an overlay: extract to repo root to overwrite docs/ops/gpt prompt files.

Recommended apply steps are provided by ChatGPT as PowerShell commands (backup → Expand-Archive → git add/commit).
